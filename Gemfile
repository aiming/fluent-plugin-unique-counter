source 'https://rubygems.org'

# Specify your gem's dependencies in fluent-plugin-unique-counter.gemspec
gemspec

if ENV['fluentd_version'] && !ENV['fluentd_version'].empty?
  gem 'fluentd', ENV['fluentd_version']
end
